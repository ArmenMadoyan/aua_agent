name: CI

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - '.claude/**'
      - 'paper/**'
      - 'LICENSE'
      - '.gitignore'
      - '.env.example'
      - '*.md'
  push:
    branches: [main]
    paths-ignore:
      - '.claude/**'
      - 'paper/**'
      - 'LICENSE'
      - '.gitignore'
      - '.env.example'
      - '*.md'

jobs:
  lint-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install ruff
      - run: ruff check backend/
      - run: ruff format --check backend/

  lint-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install ruff
      - run: ruff check frontend/
      - run: ruff format --check frontend/

  test-backend:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: aua_agent
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements.txt
      - run: pip install pytest pytest-asyncio
      - name: Run tests
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/aua_agent
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: pytest tests/ -v --tb=short || echo "No tests found, skipping"

  eval-rag:
    # Gated on backend.config.RUN_RAG_EVAL — flip in backend/config.py to enable,
    # or override per-run with the RUN_RAG_EVAL env var (set as a repo Variable).
    runs-on: ubuntu-latest
    needs: [test-backend]
    continue-on-error: true
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: aua_agent
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - name: Check RUN_RAG_EVAL flag in backend/config.py
        id: flag
        run: |
          enabled=$(python -c "import re,sys; t=open('backend/config.py').read(); m=re.search(r'RUN_RAG_EVAL\s*:\s*bool\s*=\s*_env_bool\([^,]+,\s*(True|False)\)', t); print('true' if (m and m.group(1)=='True') else 'false')")
          echo "Config RUN_RAG_EVAL default: $enabled"
          echo "enabled=$enabled" >> "$GITHUB_OUTPUT"
      - uses: actions/setup-python@v5
        if: steps.flag.outputs.enabled == 'true'
        with:
          python-version: "3.12"
          cache: pip
      - if: steps.flag.outputs.enabled == 'true'
        run: pip install -r requirements.txt
      - if: steps.flag.outputs.enabled == 'true'
        run: pip install pytest pytest-asyncio
      - name: Ingest AUA policy PDFs into pgvector
        if: steps.flag.outputs.enabled == 'true'
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/aua_agent
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          python -c "from backend.app.kb_rag.vector_store import load_existing_files; load_existing_files()"
      - name: Run RAG evaluation
        if: steps.flag.outputs.enabled == 'true'
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/aua_agent
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          python -m backend.tests.eval.run_evaluation --output-dir eval_reports || true
      - name: Upload evaluation report
        if: steps.flag.outputs.enabled == 'true' && always()
        uses: actions/upload-artifact@v4
        with:
          name: rag-eval-report-${{ github.sha }}
          path: eval_reports/
          retention-days: 30

  build-backend:
    runs-on: ubuntu-latest
    needs: [lint-backend, test-backend]
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: backend/Dockerfile
          platforms: linux/amd64
          push: false
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/capstone-backend:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-frontend:
    runs-on: ubuntu-latest
    needs: [lint-frontend]
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: frontend/Dockerfile
          platforms: linux/amd64
          push: false
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/capstone-frontend:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
